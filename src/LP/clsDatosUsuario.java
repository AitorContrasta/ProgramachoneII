package LP;


/**
 * Esta clase permite crear una contrasena y un usuario para poder hacer un
 * inicio de sesion.
 * 
 * @author Aitor Contrasta y Asier Sojo
 *
 */

public class clsDatosUsuario {

	public clsDatosUsuario() {

	}

	// Declaramos las variables.

	String user = "";
	String pass = "";

	/**
	 * En este metodo comparamos lo que nos ha escrito el usuario de la aplicacion a
	 * ver si es correcto o no con los valores que le hemos introducido nostros para
	 * poder hacer el incio de sesion.
	 * 
	 * @return
	 */
	public int probarPass() {

		/**
		 * user = clsMenuContrasena.txtUser.getText();
		 
		pass = clsMenuContrasena.pssPass.getText();

		if (user.equals("admin") && pass.equals("admin")) {
*/
			return 1;

		//} else
			//return 0;
	}

}
